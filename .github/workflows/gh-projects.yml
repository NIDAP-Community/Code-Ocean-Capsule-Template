name: TechDev-project

on:
  issues:
    types:
      - opened
  pull_request:
    types:
      - opened

permissions:
  issues: write
  pull-requests: write
  contents: read

jobs:
  CCBR-projects:
    runs-on: ubuntu-latest
    steps:
      - name: Generate a token
        id: generate-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.CCBR_BOT_APP_ID }}
          private-key: ${{ secrets.CCBR_BOT_PRIVATE_KEY }}
          owner: CCBR

      - name: add to CCBR techdev project
        uses: actions/add-to-project@v2
        with:
          project-url: https://github.com/orgs/CCBR/projects/17
          github-token: ${{ steps.generate-token.outputs.token }}

  NIDAP-projects:
    runs-on: ubuntu-latest
    steps:
      - name: Generate a token
        id: generate-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.CCBR_BOT_APP_ID }}
          private-key: ${{ secrets.CCBR_BOT_PRIVATE_KEY }}
          owner: NIDAP-Community

      - name: add to syncweaver-orchestrator project
        uses: actions/add-to-project@v2
        if: github.event_name == 'pull_request'
        with:
          project-url: https://github.com/orgs/NIDAP-Community/projects/4
          github-token: ${{ steps.generate-token.outputs.token }}
